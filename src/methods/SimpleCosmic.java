package methods;

import task_structure.TreeScript;

public class SimpleCosmic extends DefaultWalkMethod {
    SimpleCosmic(final TreeScript handler, final int id, final String runeType) {
        super(handler, id, runeType);
        // Varp 147 == 6 when completed Lost city
    }
}
